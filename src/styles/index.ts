import { baseStyles } from "./base";
import { execBlockStyles } from "./exec-blocks";
import { uiStyles } from "./ui";
import { themeStyles } from "./themes";
import { quizStyles } from "./quiz";
import { mobileStyles } from "./mobile";
import { viewerStyles } from "./viewers";

export const styles = `${baseStyles}${execBlockStyles}${uiStyles}${themeStyles}${quizStyles}${mobileStyles}${viewerStyles}`;
